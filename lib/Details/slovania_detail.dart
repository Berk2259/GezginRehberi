import 'package:flutter/material.dart';

class SlovaniaDetail extends StatelessWidget {
  const SlovaniaDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}