import 'package:flutter/material.dart';

class MontenegroDetail extends StatelessWidget {
  const MontenegroDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}